# Knowledge Base Collections (user's private documents)
*_kb/
*/kb/
collections/
!config/canaries/
!config/canaries/*/

# FTS Databases (generated from user's documents)
data/*.db
data/*.db-shm
data/*.db-wal
*.db
*.db-shm
*.db-wal

# User-specific MCP configuration (contains local paths)
.mcp.json
mcp.json
claude_desktop_config.json

# But keep example templates
!.mcp.json.example
!claude_desktop_config.json.example

# Claude Code settings (contains local paths and permissions)
.claude/

# Codex CLI settings (local paths/approvals)
.codex/*
!.codex/config.toml.example
!.codex/AGENTS.md

# Python virtual environment
.venv/
venv/
env/
ENV/

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Document conversion cache
Markitdown/
markitdown_cache/
.ingest_cache/
.cache/
data/hf_cache/
data/torch_extensions/
data/ingest_plans/
data/ingest_artifacts/

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.pytest_cache/

# Logs
*.log
logs/
mcp-logs-*/

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS files
Thumbs.db
Desktop.ini
*.lnk

# Temporary files
*.tmp
*.temp
*.bak
*.backup
.~*

# Environment variables (may contain sensitive info)
.env

# Docker volumes (if using local docker-compose overrides)
docker-compose.override.yml

# Qdrant storage (if running without docker)
qdrant_storage/

# Evaluation gold sets (stored locally to avoid leaking source text)
eval/gold_sets/*.jsonl
eval/gold_sets/*.json
!eval/gold_sets/README.md
!eval/gold_sets/*_template.jsonl
!eval/gold_sets/*_auto.jsonl
